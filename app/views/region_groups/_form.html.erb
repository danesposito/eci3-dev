<%= simple_form_for(@region_group) do |f| %>
  <%= f.error_notification %>
  <%= f.association :region_source %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
