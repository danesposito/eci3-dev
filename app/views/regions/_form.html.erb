<%= simple_form_for(@region) do |f| %>
  <%= f.association :region_group %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :slug %>
  <%= f.association :countries, as: :check_boxes %>
  <%= f.button :submit %>
<% end %>
