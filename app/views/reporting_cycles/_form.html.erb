<%= form_for(@reporting_cycle) do |f| %>
  <% if @reporting_cycle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reporting_cycle.errors.count, "error") %> prohibited this reporting_cycle from being saved:</h2>

      <ul>
      <% @reporting_cycle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :convention_id %><br>
    <%= f.collection_select :convention_id, Convention.order(:name),:id,:name, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
