<%= simple_form_for(@convention_membership) do |f| %>
  <%= f.association :convention %>
  <%= f.association :country %>
  <%= f.association :membership_type %>
  <%= f.input :date %>
  <%= f.button :submit %>
<% end %>
