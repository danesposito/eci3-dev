<p id="notice"><%= notice %></p>

<h1></h1>

<table>
  <thead>
    <tr>
      <th>Region</th>
      <th>Country</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @region_countries.each do |region_country| %>
      <tr>
        <td><%= region_country.region.name %></td>
        <td><%= region_country.country.name %></td>
        <td><%= link_to 'Show', region_country %></td>
        <td><%= link_to 'Edit', edit_region_country_path(region_country) %></td>
        <td><%= link_to 'Destroy', region_country, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Region country', new_region_country_path %>
